spring:
  application:
    name: Opportunity-API
  datasource:
    url: jdbc:postgresql://postgres:tsCysGadzZoOVXQamrxIlOCjAjrcKLmI@autorack.proxy.rlwy.net:25270/railway
    username: postgres
    password: tsCysGadzZoOVXQamrxIlOCjAjrcKLmI
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update  # Use 'update' para ajustar o esquema automaticamente. Pode usar 'validate', 'create', ou 'create-drop' conforme necess�rio.
    show-sql: true      # Para mostrar as queries SQL no console (opcional)
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
